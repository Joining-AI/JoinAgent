# 这段代码是微软公司的一个程序，用于创建一个全球搜索系统的提示信息。
# 它有三个主要部分：版权信息、一段描述和两个字符串变量。

# 版权信息（不能删除）
# 这一行表示这个代码的版权归微软公司所有，且遵循MIT许可证。
# 注：这行代码不会影响程序的实际运行，只是一个法律声明。

# 描述部分（不能删除）
# 这是一个多行字符串，定义了“全局搜索系统提示”的规则。
# 1. 角色：你是一个助手，回答关于数据集的问题，综合多位分析师的观点。
# 2. 目标：根据用户的问题，生成目标长度和格式的回答，总结多个分析师关于数据集不同部分的报告。
# 3. 注意事项：按重要性降序排列分析师的报告，如果不知道答案或报告信息不足，就直接说不知道。
# 4. 响应要求：去除无关信息，合并关键点，保持原意，使用适当的模态动词，保留数据引用，但不要提及分析师的角色。
# 5. 示例：如何列出数据记录ID，并在超过5个时用"+more"表示还有更多。
# 6. 响应长度和格式：需要按照指定的响应类型和格式来生成回答。
# 7. 报告示例：再次强调报告的重要性排序和处理方式。

# 定义一个变量，用于没有数据时的回答（不能删除）
# 当没有可用的数据来回答问题时，程序会返回这个字符串。

# 另一个变量，关于使用外部知识的指示（不能删除）
# 如果回答可以包含额外的真实世界知识，但必须用[LLM: verify]标签明确标注。

# 这两行定义的字符串变量不会被执行，只是作为程序的一部分数据存储在那里。

